# 留守儿童情绪手账分析系统：队友部署说明

## 1. 这个项目能不能部署？

可以部署，难度不高。推荐两种方式：

- 本地部署：队友在自己的 Windows 电脑上运行，适合支教队内部使用。
- 云端部署：放到服务器或 Streamlit Cloud，别人通过网址访问，适合多人共同使用。

当前项目使用了 Windows 本地 OCR，所以最稳的部署环境是 Windows 10/11。若部署到 Linux 云服务器，图片文字识别部分需要改成其他 OCR 服务或安装额外 OCR 组件。

## 2. 队友电脑需要准备什么？

1. Windows 10 或 Windows 11。
2. Python 3.10 或以上版本。
3. 浏览器，例如 Edge 或 Chrome。
4. 讯飞开放平台账号，并开通 Spark X2/X1.5 HTTP 服务。
5. 自己的讯飞 API 信息：
   - APPID
   - APIKey
   - APISecret
   - HTTP 服务 APIPassword

## 3. 下载项目

如果项目已经放到 GitHub，队友可以打开终端运行：

```powershell
git clone 项目仓库地址
cd 情绪手账
```

如果暂时不用 GitHub，也可以把整个项目文件夹压缩发给队友。注意不要把你的 `.env` 文件一起发过去。

## 4. 安装依赖

在项目目录里运行：

```powershell
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```

如果提示 `pip` 下载慢，可以换国内镜像：

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 5. 配置自己的 API 密钥

项目里有一个 `.env.example` 文件。

队友需要复制一份并改名为 `.env`：

```powershell
copy .env.example .env
```

然后用记事本或 VS Code 打开 `.env`，填入自己的讯飞信息：

```env
XUNFEI_APP_ID=自己的APPID
XUNFEI_API_KEY=自己的APIKey
XUNFEI_API_SECRET=自己的APISecret
XUNFEI_API_PASSWORD=自己的HTTP服务APIPassword
```

注意：

- `.env` 是私密文件，不要上传 GitHub。
- 如果 API 调用失败，优先检查 `XUNFEI_API_PASSWORD` 是否填的是 HTTP 服务认证信息，不是 WebSocket 的 APISecret。

## 6. 启动系统

在项目目录里运行：

```powershell
streamlit run app.py
```

启动后浏览器会打开类似下面的网址：

```text
http://localhost:8501
```

如果没有自动打开，可以手动复制到浏览器。

## 7. 使用方法

1. 在左侧填写孩子基本信息。
2. 上传情绪手账图片。
3. 点击“开始分析”。
4. 系统会进行：
   - 本地 OCR 识别图片文字；
   - 图像颜色、亮度、留白、线条密度分析；
   - 调用讯飞 Spark 生成情绪和心理关怀报告；
   - 生成情绪雷达图和情绪分布图。
5. 在“报告下载”页面下载 Word 报告。

## 8. 常见问题

### 8.1 点不了“开始分析”

确认是否已经上传图片。现在系统设置为“上传图片后即可分析”，姓名不是必填。

### 8.2 分析失败或 API 报错

检查 `.env`：

- `XUNFEI_API_PASSWORD` 是否填写；
- 是否开通 Spark X2/X1.5 HTTP 服务；
- API 服务是否还有 token 余额；
- 网络是否能访问讯飞接口。

### 8.3 OCR 识别不准

建议上传更清晰的图片：

- 尽量正对纸面拍摄；
- 避免阴影和反光；
- 手写字不要太小；
- 图片中尽量只包含手账页面，不要截到电脑界面或侧栏。

### 8.4 Word 里没有图

重新分析后再下载 Word 报告。旧报告不会自动更新。

## 9. 如何让别人通过互联网访问？

有三种方式：

### 方式一：本地局域网访问

适合在同一个 Wi-Fi 下使用。

运行：

```powershell
streamlit run app.py --server.address 0.0.0.0
```

然后让队友访问你的电脑局域网 IP，例如：

```text
http://你的局域网IP:8501
```

缺点：只能同一网络访问。

### 方式二：部署到 Streamlit Community Cloud

适合公开演示。

步骤：

1. 把项目上传到 GitHub。
2. 登录 Streamlit Community Cloud。
3. 选择 GitHub 仓库。
4. 设置入口文件为 `app.py`。
5. 在 Streamlit Cloud 的 Secrets 中配置讯飞 API 信息。

注意：云端是 Linux 环境，当前 Windows OCR 可能不可用。如果要云端稳定识别图片文字，建议改接一个云 OCR 服务。

### 方式三：部署到云服务器

适合正式多人使用。

可以购买 Windows 云服务器，然后按本说明安装 Python、依赖和 `.env`，再运行：

```powershell
streamlit run app.py --server.address 0.0.0.0 --server.port 8501
```

同时需要在云服务器安全组开放 8501 端口。

## 10. 上传 GitHub 前必须检查

上传前确认不要包含：

- `.env`
- `api_debug.log`
- `test_result.txt`
- `__pycache__`
- `.idea`

本项目已提供 `.gitignore`，正常使用 Git 时会自动忽略这些文件。

推荐上传的文件包括：

- `app.py`
- `requirements.txt`
- `.env.example`
- `.gitignore`
- `utils/`
- `队友部署说明.md`
- `analysis_method_basis.docx`

## 11. GitHub 上传参考命令

第一次上传：

```powershell
git init
git add .
git commit -m "Initial emotion handbook analysis app"
git branch -M main
git remote add origin 你的GitHub仓库地址
git push -u origin main
```

后续更新：

```powershell
git add .
git commit -m "Update app"
git push
```

如果你不熟悉 GitHub，建议先在 GitHub 网页新建空仓库，再复制仓库地址。

